package ma.hps.powercard.compliance.accessimpl;

/**
 * Implementation of Access object for Multi_lang_valuesRepository.getSequenceNextVal.
 *
 */
public class GetSequenceNextValAccessImpl {
	/*
    public void performExecute() {

        Query query = getEntityManager().createNativeQuery("select multi_lang_values_x.nextval from dual");
        setResult(query.getResultList().get(0).toString());
    }
    */
}
